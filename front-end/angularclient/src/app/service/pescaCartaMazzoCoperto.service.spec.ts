/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { PescaCartaMazzoCopertoService } from './pescaCartaMazzoCoperto.service';

describe('Service: PescaCartaMazzoCoperto', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [PescaCartaMazzoCopertoService]
    });
  });

  it('should ...', inject([PescaCartaMazzoCopertoService], (service: PescaCartaMazzoCopertoService) => {
    expect(service).toBeTruthy();
  }));
});
